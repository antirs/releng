CFLAGS_HARDENED_SSP="-fstack-protector-strong --param ssp-buffer-size=4"
CFLAGS_HARDENED="${CFLAGS_HARDENED} ${CFLAGS_HARDENED_SSP}"

CFLAGS="${CFLAGS} ${CFLAGS_HARDENED_SSP}"
CXXFLAGS="${CFLAGS}"
FCFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"
