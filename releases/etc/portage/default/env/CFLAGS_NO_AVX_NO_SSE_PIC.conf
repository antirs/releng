CFLAGS_CPU="-march=haswell -mmmx"
CFLAGS="${CFLAGS_COMMON} ${CFLAGS_COMPILER} ${CFLAGS_CPU} ${CFLAGS_SSP} ${CFLAGS_FORTIFY}"
CXXFLAGS="${CFLAGS}"
FCFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"

LDFLAGS="${LDFLAGS_COMMON}"

CPU_FLAGS_X86="mmx mmxext pclmul popcnt"
